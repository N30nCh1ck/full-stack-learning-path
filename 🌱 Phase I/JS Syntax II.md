>**Ref:** [[JS Syntax II]]

**Function**
- create `function` that gets `number` then returns is it a `even` number or not? (`true` or `false`)
- create `function` that gets `number` then returns by the following condition:
	- `A` when `number` >= 80
	- `B` when `number` >= 70
	- `C` when `number` >= 60
	- `D` when `number` >= 50
	- otherwise return `F`

**Array**
- create a `function` that gets `value` then check is it an `array` or not?
- change `[1,2,3,4,5]` to `[-1,0,3,8,15]` by using `array.map((item,index) => {})` 
- filter `[1,2,3,4,5]` to `[1,3,5]` by using `array.filter(item => {})`
- sum all value in `[1,2,3,4,5]` then return `number` using `array.reduce((prev, next) => {}, initialvalue)`

**Math**
- create a `function` that returns random `number` between `1-10`

**RegExp**
- create a validate `function` that return `true` when input "text" contain only `A-Z`, `a-z`, or `0-9` using `RegExp` 

**Set**
- create a `function` that remove duplicate values from `[1,2,1,3,4,3,4,1,2,1,4,3]` using `Set` then returns `[1,2,3,4]`

**Map**
- create a `function` that gets the following input `John`, `25`, `false` as params then returns as `Map` value `Name`, `Age`, `Is Student` by params order


#javascript #phase-1 #assignment #syntax